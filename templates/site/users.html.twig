{% extends 'base.html.twig' %}

{% block body %}


    <br><h4 class="card-header">Utilisateurs</h4><br>
    <div class="container">
    <form action="" method="post" >
        <div class="row">
            <div class="col-sm"></div>

                <div class="form-group col-sm">
                    <label for="exampleFormControlInput1">Login</label>
                    <input type="text" class="form-control" name="username" id="exampleFormControlInput1" placeholder="login"><br>

                <button type="submit" name="search" class="btn btn-success">Rechercher</button>
            </div>
            <div class="col-sm"></div>

        </div>

    </form><br><br>
    </div>
    {% for user in users %}

        {% if user.email == app.user.email %}

        {% else %}
        <div class="container">
            <div class="card">

                <div class="card-body">
                    <h5 class="card-title">Login : {{ user.username }}</h5>
                    <h5 class="card-title">Email : {{ user.email }}</h5>
                    <h5 class="card-title">Race : {{ user.race }}</h5>
                    <h5 class="card-title">Famille : {{ user.famille }}</h5>
                    <h5 class="card-title">Taille : {{ user.taille }} cm </h5>
                    <h5 class="card-title">Age : {{  user.age | age }} ans </h5>


                        {% if user in amis  %}
                            <button type="" class="btn btn-success">Déjà ajouté</button>

                        {% else %}
                        <form action="" method="post">
                            <input  type='hidden' name="ida" value="{{ user.id }}">
                            <input type="hidden" name="idu" value="{{ app.user.id }}">
                             <button type="submit" name="submit" class="btn btn-success">Ajouter dans vos amis</button>

                        </form>
                        {% endif %}


                </div>

            </div>
        </div>
            {% if app.request.get('add') is defined %}

            {% endif %}
        {% endif %}

    {% endfor %}
{% endblock %}